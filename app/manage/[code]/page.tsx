'use client'; import { use, useEffect, useState } from 'react';
export default function Manage({params}:{params:Promise<{code:string}>}){const {code}=use(params);const [data,setData]=useState<any>();const [error,setError]=useState('');useEffect(()=>{fetch(`/api/rsvp/${code}`).then(r=>r.json().then(x=>r.ok?setData(x):setError(x.error)))},[code]);
 async function cancel(){if(!confirm('Cancel this RSVP?'))return;await fetch(`/api/rsvp/${code}`,{method:'DELETE'});location.reload()}
 return <main className="shell section"><div className="card">{error?<p className="notice error">{error}</p>:!data?<p>Loading…</p>:<><div className="eyebrow">Your nesting party RSVP</div><h1 className="title" style={{fontSize:'3.2rem'}}>You’re on the <span className="script">crew</span>!</h1>{data.status==='cancelled'&&<p className="notice error">This RSVP has been cancelled.</p>}<p><strong>{data.name}</strong> · Party of {data.party_size}</p><p><strong>Time:</strong> {data.arrival_time}–{data.departure_time}</p><p><strong>Tasks:</strong> {data.task_signups?.length?data.task_signups.map((x:any)=><span className="pill" key={x.task_id}>{x.tasks?.title}</span>):'Place me where needed'}</p><p className="notice">Save this private page. It is your link for viewing or cancelling your RSVP. The exact location can be added here once confirmed.</p>{data.status!=='cancelled'&&<button className="btn danger" onClick={cancel}>Cancel RSVP</button>}</>}</div></main>}
